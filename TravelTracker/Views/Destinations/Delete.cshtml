@{
  Layout = "_Layout";
}

@model TravelTracker.Models.Destination

<h3>@Html.Label($"Are you sure you want to delete {Model.CityName}?")</h3>

@using (Html.BeginForm())
{
  <input type="submit" value="Delete" class="btn btn-outline-dark">
}
<hr />
<h5>@Html.ActionLink("Back to Destinations", "Index")</h5>