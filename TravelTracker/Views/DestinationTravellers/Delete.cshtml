@{
  Layout = "_Layout";
}

@model TravelTracker.Models.DestinationTraveller

<h3>@Html.Label("Are you sure you want to delete this trip?" )</h3>
<hr />

@using (Html.BeginForm())
{
  <input type="submit" value="Delete" class="btn btn-outline-dark">
}
<hr />
<h5>@Html.ActionLink("Back to Home", "Index", "Home")</h5>